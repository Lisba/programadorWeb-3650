var numerouno = parseInt(prompt("Escriba un número"))
var numerodos = parseInt(prompt("Escriba otro número"))

if(numerouno > numerodos) {
  document.write("El primer número que escribiste es mayor que el segundo")
} else {
  document.write("El segundo número que escribiste es mayor que el primero")
}